# Neon Void

Standalone HTML5 arcade game with 10 stages, including Stage 10: The Neon Void Prototype. Bosses are rendered with canvas/code shapes. Open `index.html` in a browser, or run a tiny local server:

```sh
python3 -m http.server 8080
```

Then visit http://localhost:8080.
